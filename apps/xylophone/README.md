# Xylophone

Xylophone app that plays sounds on key tap, from the dependencies module of
the 2019 Flutter Development Bootcamp. Uses the `audioplayers` package.

```bash
flutter pub get
flutter run
```

Run the tests with `flutter test`.
