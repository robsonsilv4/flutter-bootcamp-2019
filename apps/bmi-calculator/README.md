# BMI Calculator

BMI calculator with height and weight sliders, from the layout and navigation
module of the 2019 Flutter Development Bootcamp.

```bash
flutter pub get
flutter run
```

Run the tests with `flutter test`. The widget tests need a phone-sized
viewport; `test/helpers/pump_app.dart` already sets it.
